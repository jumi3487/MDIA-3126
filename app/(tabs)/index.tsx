import { View } from "react-native";
import inputForm from "@/components/molecules/inputForm";

export default function Index() {
  return (
    <View
      style={{
        flex: 1,
        justifyContent: "center",
        alignItems: "center",
      }}
    >
      TODO: Add links to other routes
    </View>
  );
}
